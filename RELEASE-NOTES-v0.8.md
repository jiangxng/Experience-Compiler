# EC v0.8 Release Notes

v0.8 advances the executable v0.7 loop toward production-shaped integration.

Added: PostgreSQL canonical adapter, rebuildable projection fan-out, OpenAI-compatible model adapter,
bounded HTTP research acquisition, configurable EVO/Eidos HTTP preview gateways, expanded manufacturing
ontology/decision/KPI/exception catalogs, and integration regression tests.

Maturity statement: production-shaped does not mean production-certified. Real EVO/Eidos endpoints,
credentials, load tests, HA PostgreSQL/object/search/vector infrastructure and manufacturing corpus depth
must still be validated in their owning environments.
