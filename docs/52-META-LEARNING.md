# Meta-learning

Learning methods are data. Preserve strategy ID/version, steps, model routing, thresholds, review rules and empirical evaluations. Improvements become new versions. Never permit an LLM to silently rewrite the active learning constitution.
