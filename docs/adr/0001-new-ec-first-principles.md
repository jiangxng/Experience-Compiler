# ADR-0001: New EC is a first-principles architecture restart

Status: Accepted.

The historical Experience-Compiler prototype is not an implementation compatibility constraint. Preserve it externally as history if desired. New EC optimizes for persistent enterprise intelligence over decades.
