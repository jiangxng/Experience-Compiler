# ADR-0003: Models are replaceable reasoning processors

Status: Accepted.

Knowledge, learning methods and authority rules persist independently of provider/model. Model migration uses frozen context benchmarks and staged rollout.
