# Model Routing

EC is model-aware but model-unbound. Registry profiles may include context size, strengths, modalities, residency, tool support, structured output, latency/cost and approved risk classes. A cheap model can triage; a strong model can synthesize; a separate model can criticise. High-risk tasks may require cross-model review.
