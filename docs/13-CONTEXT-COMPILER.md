# Context Compiler

The compiler optimizes decision value and information gain, not minimum tokens. It may iteratively retrieve more evidence when uncertainty/contradictions remain. Context packs include omissions so a model knows what was not retrieved.

Future scoring should combine scope authorization, authority, temporal validity, semantic relevance, lexical relevance, graph proximity, case similarity, novelty and contradiction coverage.
