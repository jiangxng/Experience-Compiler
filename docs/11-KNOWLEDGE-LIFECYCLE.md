# Knowledge Lifecycle

`raw -> claim -> candidate -> accepted/disputed -> superseded/archive`. Promotion is policy-driven, never implied by ingestion. High-impact policy knowledge may require human review.

Important knowledge preserves source, hash, retrieval time, publisher, method, model/strategy version, temporal validity, confidence and parent lineage.
