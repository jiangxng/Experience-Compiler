# Quality Gates

No release when: schemas are ambiguous; migration cannot rollback/rebuild; tenant isolation tests fail; provenance is missing for governed knowledge; a new model lacks benchmark results; Eidos fallback is absent; EVO command contract is bypassed; backup restore drill is overdue for production release.
