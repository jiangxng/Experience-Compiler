{
  "name": "experience-compiler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node tests/m0.test.mjs",
    "example": "npm run build && node scripts/example.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
